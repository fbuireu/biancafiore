---
import { isWithin } from "@modules/core/utils/pathname";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href, class: classNames, ...props } = Astro.props;

const isCurrentPage = isWithin(Astro.url.pathname, String(href));
---
<a
    href={href}
    class:list={[`clickable ${classNames}`, { 'current-page': isCurrentPage }]}
    aria-current={isCurrentPage ? 'page' : undefined}
    {...props}
>
  <slot />
</a>
