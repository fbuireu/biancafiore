---
import { authorDTO } from "@application/dto/author";
import type { AuthorDTO, RawAuthor } from "@application/dto/author/types";
import type { RawCity } from "@application/dto/city/types";
import AboutIntro from "@components/molecules/aboutIntro/AboutIntro.astro";
import Breadcrumbs from "@components/molecules/breadcrumbs/Breadcrumbs.astro";
import AboutLatestArticles from "@components/organisms/aboutLatestArticles/AboutLatestArticles.astro";
import { LittleMoreOfMe } from "@components/organisms/littleMoreOfMe/index";
import BaseLayout from "@components/templates/baseLayout/BaseLayout.astro";
import { CONTACT_DETAILS } from "@const/index";
import { client } from "@lib/contentful";
import { cityDTO } from "src/application/dto/city";

const [{ items: rawAuthors }, { items: rawCities }] = await Promise.all([
	client.getEntries<RawAuthor>({ content_type: "authors" }) as Iterable<PromiseLike<RawAuthor[]> | RawAuthor[]>,
	client.getEntries<RawCity>({
		content_type: "city",
		order: ["fields.startDate"],
	}) as Iterable<PromiseLike<RawCity[]> | RawCity[]>,
]);

const cities = cityDTO.render(rawCities);

const bianca = (await authorDTO.render(rawAuthors as RawAuthor[]))
	.filter((author) => author.name === CONTACT_DETAILS.NAME)
	.at(0) as AuthorDTO;
---

<BaseLayout title="" description="">
    <Breadcrumbs classNames="about-page" />
    <AboutIntro />
    <LittleMoreOfMe client:only="react" cities={cities} />
    <AboutLatestArticles />
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'ProfilePage',
      'mainEntity': {
        '@id': '#bianca',
        '@type': 'Person',
        'name': bianca.name,
        'url': new URL(Astro.url).href,
        'image': new URL(bianca.profileImage.url, Astro.url).href,
        'jobTitle': bianca.jobTitle,
        'worksFor': {
          '@type': 'Organization',
          'name': bianca.currentCompany,
        },
      },
      'hasPart': [
        {
          '@type': 'Article',
          'headline': bianca.latestArticle.title,
          'url': new URL(`articles/${bianca.latestArticle.slug}`, Astro.url).href,
          'datePublished': bianca.latestArticle.publishDate,
          'author': { '@id': '#bianca' },
        }],
      'sameAs': [...bianca.socialNetworks],
    })} />
</BaseLayout>
