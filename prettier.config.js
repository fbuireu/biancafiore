/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'es5',
  arrowParens: 'always',
  proseWrap: 'always',
  xmlWhitespaceSensitivity: 'ignore',
  bracketSpacing: true,
};

export default config;
